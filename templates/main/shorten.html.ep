% layout 'default';
%= form_for 'main#shorten' => ( method => 'POST', role => "form" ) => begin
%=  bs_form_group [longurl => 'url', placeholder => l('Enter URL to shorten...'), required => "required"] => ( label => l('Link to shorten') )
%=  bs_submit l('Shorten')
%= end
