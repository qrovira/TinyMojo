% layout 'default';
% title $config->{site_mission};
%= form_for 'main#shorten' => ( method => 'POST', role => "form" ) => begin
%=  bs_form_group [longurl => 'text', placeholder => loc('Enter URL to shorten...')] => ( label => loc('Link to shorten') )
%=  bs_submit loc('Shorten')
%= end
