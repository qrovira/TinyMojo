% layout 'default';
% title l('Dashboard - [_1]', $title);

%= bs_nav "nav nav-tabs" => begin
%=  bs_nav_item l('Dashboard') => 'user#dashboard';
%=  bs_nav_item l('Profile') => 'user#profile';
%   if( admin ) {
%=   bs_nav_item l('List all URLs') => 'user#list_urls';
%   }
%=  bs_nav_item l('Log out') => 'user#logout';
%= end

%= content main => begin
<!-- no content -->
% end
