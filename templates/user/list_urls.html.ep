% extends 'user/base', title => l('Last URLs');
% content main => begin

<h3><%= l('List of last [_1] short URLs generated', $rows) %></h3>
%= include 'user/blocks/url_list', urls => $urls, hits => $hits, show_user => 1

% end
