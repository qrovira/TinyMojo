% layout 'default';
% title l('Shorten');

%= form_for 'url#shorten' => ( method => 'POST', role => "form" ) => begin
%=  bs_form_group [longurl => 'url', placeholder => l('Enter URL to shorten...'), required => "required"] => ( label => l('URL') )
%=  bs_submit l('Shorten')
%= end
